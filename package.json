{
  "name": "math-lib",
  "version": "1.0.0",
  "description": "Biblioteca de funções matemáticas para exercícios de testes unitários com Vitest",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage":"vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "axios": "^1.20.0"
  }
}
